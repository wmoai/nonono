---
description: 開発ルール
---

# 実装の計画

なんらかの開発を行う際は、必ず `/plan` skill で計画を立ててから行う。

# コーディング規約

- linter は hook により都度自動実行されるので、エラーが出たら修正すること
- 変数名は一見して実態が想像できる名前をつける。`u`, `i` などの省略形は原則避けること

# ドキュメント

- ドキュメントを出力する必要がある場合は、`./agent-docs` に出力する
